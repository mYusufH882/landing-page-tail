const { colors } = require("tailwindcss/defaultTheme")

module.exports = {
  // prefix: "lus-",
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
